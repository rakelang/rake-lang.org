# Website publishing

Before every push, cache-bust changed browser assets and update all consuming
references in the same changeset. Use a new asset filename or bump its URL query
version so browsers request the updated CSS, JavaScript, images, and other assets.
